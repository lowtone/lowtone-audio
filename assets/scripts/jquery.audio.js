// Generated by CoffeeScript 1.5.0
(function() {
  var $, a, m;

  $ = jQuery;

  a = audiojs;

  m = Modernizr;

  $(function() {
    return $('audio').each(function() {
      var $audio, basename, src;
      if (m.audio) {
        return a.create(this);
      } else {
        $audio = $(this);
        src = $audio.find('source').attr('src');
        basename = src.substr(src.lastIndexOf('/') + 1);
        return $audio.replaceWith('<a href="' + src + '">' + basename + '</a>');
      }
    });
  });

}).call(this);
